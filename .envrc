#!/usr/bin/env bash

# Use the Nix flake development shell
use flake

# Set up Emscripten cache directory in project
export EM_CACHE="$(pwd)/.emscripten_cache"

# Ensure cache directory exists
mkdir -p "$EM_CACHE"

# Add WASM build output to PATH for easy access
export PATH="$(pwd)/result/lib:$PATH"

echo "✅ Development environment loaded via direnv"